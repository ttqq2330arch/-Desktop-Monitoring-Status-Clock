# -Desktop-Monitoring-Status-Clock
A tiny desktop status display: an ESP32 + 1.8" ST7735S showing live CPU/RAM/NET/GPU/DISK stats pushed from a Python host over USB serial at 1 Hz. Four pages (Minecraft-styled overview, flip clock, calendar, weather), one-button switching, auto-start on login, and a portable build that needs no Python.
